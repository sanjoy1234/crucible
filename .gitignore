# Runtime artifacts
.last_report_id

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
*.egg
*.egg-info/
dist/
build/
.eggs/
*.whl
MANIFEST

# Virtual environments — NEVER commit these
.venv/
venv/
env/
ENV/
.env

# Testing
.pytest_cache/
.coverage
htmlcov/
.tox/
*.log

# CRUCIBLE runtime data — local state, not source code
.crucible/reports/
.crucible/forge/
.crucible/brain/
# Keep .crucible.yml (config template) — DO commit it

# IDE / OS
.DS_Store
.idea/
.vscode/settings.json
*.swp
~$*

# Internal working documents — not for public repo
*.docx
*.xlsx
*.pptx

# Secrets — never commit
.env.local
*.pem
secrets.json

# Claude Code workspace config — not for public repo
CLAUDE.md
